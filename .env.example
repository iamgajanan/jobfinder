# Backend API running on the same Raspberry Pi/server as Next.js.
# The Next.js route proxies browser requests server-side, avoiding browser CORS.
JOBS_API_URL=http://127.0.0.1:8004/api/v1/jobs/search
