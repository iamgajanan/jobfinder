# Server-side URL used by the Next.js backend proxy.
# Production:
BACKEND_API_URL=https://jobs.n8npi.live/api/v1

# Local development, when the FastAPI backend is running on port 8004:
# BACKEND_API_URL=http://127.0.0.1:8004/api/v1
